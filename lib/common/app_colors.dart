import 'package:flutter/material.dart';

class AppColors {
  // Gradient colors for splash screen
  static const Color gradientStart = Color(0xFF6B21A8); // Purple
  static const Color gradientEnd = Color(0xFF1E3A8A);   // Blue

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Indicator colors
  static const Color indicatorActive = Color(0xFF4DA6FF);   // Blue indicator
  static const Color indicatorInactive = Color(0xFF1E3A8A); // Darker blue

  // App general colors
  static const Color scaffoldBackground = Color(0xFF081025);
  static const Color surface = Color(0xFF0E1A26);
  static const Color cardBackground = Color(0xFF0F1720);
  static const Color accent = Color(0xFF1E90FF);
  static const Color bookmarkActive = Color(0xFFFBC02D);
}
